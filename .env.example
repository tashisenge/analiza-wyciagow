# --- Lokalny dev (PostgreSQL, np. docker compose) ---
DATABASE_URL="postgresql://analiza:analiza@localhost:5432/analiza_wyciagow"
# Migracje Prisma; lokalnie ten sam URL co DATABASE_URL (bez poolera)
DIRECT_URL="postgresql://analiza:analiza@localhost:5432/analiza_wyciagow"

AUTH_SECRET="wygeneruj: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"

# --- Produkcja: Vercel + Neon (patrz docs/deploy-vercel-neon.md) ---
# DATABASE_URL — pooled (serverless), host *.pooler.neon.tech:
#   postgresql://USER:PASS@ep-xxx-pooler.region.aws.neon.tech/DB?sslmode=require
# DIRECT_URL — bez poolera (migrate deploy), host ep-xxx.region.aws.neon.tech:
#   postgresql://USER:PASS@ep-xxx.region.aws.neon.tech/DB?sslmode=require
# AUTH_SECRET — openssl rand -base64 32 (min. 32 znaki)
# NEXTAUTH_URL — https://twoja-domena.vercel.app

# AI (opcjonalnie — Claude lub OpenAI)
ANTHROPIC_API_KEY=""
# OPENAI_API_KEY=""
# AI_PROVIDER=anthropic
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# OPENAI_MODEL=gpt-4o-mini

# Wyszukiwanie alternatyw na /optimize (Tavily + AI powyżej)
# TAVILY_API_KEY=""
